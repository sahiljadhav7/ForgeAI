# 03 — Rename in-app text from ForgeAI/Forge to Daybreak

Status: ready-for-agent
Blocked by: none
Spec: `.scratch/new-frontend/spec.md` § Rename to Daybreak

## What

Replace the old brand in everything users see or download. Infrastructure (domain, GitHub repo, Clerk app, Vercel) is out of scope.

## Skills
- `/code-review`: confirm the `forge` grep returns only the allowed hits and the ZIP README has no `forge.app` link.

## Tasks
- `app/layout.tsx` metadata: title template `%s · Daybreak`, default "Daybreak", description "Daybreak turns a written description into a working React app." This replaces "Ai App Builder" / "Generated by create next app".
- `components/ChatPanel.tsx:231`, `:278`: alt text "Daybreak".
- `components/CodePanel.tsx` ZIP export:
  - package name `forge-app` → `daybreak-app` (`:207`)
  - `<title>Forge App</title>` → `Daybreak App` (`:234`)
  - the bundled README (`:266`): "# Daybreak App" and "Generated with Daybreak.", with **no link**; the current `https://forge.app` link points at a domain we don't own
  - filename `forge-app.zip` → `daybreak-app.zip` (`:278`)
- `lib/constants.ts:63`: "Access to Daybreak Pro Agent".
- `README.md`: rename the product to Daybreak throughout. Keep the live-demo URL (forgeai.lol) and the repo link as they are for now.
- Re-grep for `forge` (case-insensitive, excluding `node_modules`, `.next`, `lib/generated`, `package-lock.json`). The only remaining hits should be the forgeai.lol URLs, `scripts/capture-readme-visuals.mjs`, and the video files that ticket 04 renames.

## Acceptance criteria
- An exported ZIP is named `daybreak-app.zip`, its `package.json` name is `daybreak-app`, and its README has no `forge.app` link.
- The browser tab on non-landing pages reads "… · Daybreak" or "Daybreak".
- The grep in the last task returns only the allowed hits.
- `npm test` and `npm run build` pass. Update any test fixtures that assert on the old strings.
